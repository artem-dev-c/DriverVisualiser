# DriverVisualiser

DriverVisualiser is a Windows desktop application that visualises
installed device drivers, their versions, providers, and status.

The application is read-only and does not modify or update drivers.
